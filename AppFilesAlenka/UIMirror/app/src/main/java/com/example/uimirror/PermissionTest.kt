package com.example.uimirror
/*

import androidx.lifecycle.ViewModel

// Diese Klasse verwaltet die Berechtigungen, die für den Zugriff auf die Kamera erforderlich sind.

class PermissionTest : ViewModel() {
    //Queue [Notification, CAMERA]
    val visiblePermissionDialogQueue = mutableStateListOf<String>()

    fun dismissDialog() {
        visiblePermissionDialogQueue.removeLast()
    }

    fun on PermissionResult(permission: String, isGranted: Boolean){
        if(!isGranted) {
            visiblePermissionDialogQueue.add(0, permission)

        }
    }





}*/